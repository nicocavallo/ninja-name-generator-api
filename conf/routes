# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /healthcheck                controllers.Application.healthCheck
GET     /ninjaName/forUser/:name    controllers.Application.ninja(name: String)